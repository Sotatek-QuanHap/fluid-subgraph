{
  "name": "vesting",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service sotatek-quanhap/vesting-subgraph",
    "create-local": "graph create --node http://localhost:8020/ vesting",
    "remove-local": "graph remove --node http://localhost:8020/ vesting",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 vesting"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.32.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
